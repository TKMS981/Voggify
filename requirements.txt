# Voggify の依存パッケージ
#   pip install -r requirements.txt

# GUI
PySide6>=6.6

# テストを実行する場合は requirements-dev.txt も入れてください。

# --------------------------------------------------------------------------
# 変換エンジンは外部の ffmpeg / ffprobe 実行ファイルに依存します。
# Python パッケージではないので、別途インストールしてください。
#   Windows : winget install Gyan.FFmpeg
#   macOS   : brew install ffmpeg
#   Debian  : sudo apt install ffmpeg
#
# libvorbis を含むビルドが必要です（`python main.py check` で確認できます）。
# --------------------------------------------------------------------------
