# 開発・テスト・ビルド用の依存パッケージ
#   pip install -r requirements.txt -r requirements-dev.txt

# テスト
pytest>=7.0

# exe のビルド（pyinstaller voggify.spec）
pyinstaller>=6.0

# アイコンの生成（python assets/generate_icon.py）
# 生成した .ico はコミットしてあるので、アプリの実行時には不要
Pillow>=10.0
